-To run change arguments(giving locations of the file) in the Makefile
-After the make command terminal will ask which file to run.
	- run the model first
	- then prediction
-As the project is in spark we have submitted stderr instead of syslog
